<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.secret_hitler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".VotingActivity" />
        <activity android:name=".LobbyActivity" />
        <activity android:name=".StatusActivity" />
        <activity android:name=".ChooseChancellorActivity" />
        <activity android:name=".PresidentVotingWaitingRoomActivity" />
        <activity android:name=".PresidentSelectLawsActivity" />
        <activity android:name=".LawUnveilingActivity" />
        <activity android:name=".ChancellorPickLawActivity" />
        <activity android:name=".PresidentActionActivity" />
        <activity android:name=".LastRoundVotesActivity" />
        <activity android:name=".GameEndingActivity"></activity>
    </application>

</manifest>